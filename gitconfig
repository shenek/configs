[user]
	name = Stepan Henek
	#email = stepan.henek@seznam.cz
	#email = stepan.henek@nic.cz
	#email = stepan@henek.name
	signingkey = 647EB8F2
[core]
	editor = vim
	excludesfile = /home/stepan/.gitignore
[merge]
	tool = vimdiff

[color]
	ui = auto
	diff = auto
	branch = auto
	status = auto
	interactive = auto
[push]
	default = current
[diff "odf"]
	textconv=odt2txt

[diff "fodf"]
	textconv=odt2txt --raw-input

[log]
	decorate = full

[commit]
	gpgsign = true
	verbose = true

[fetch]
	prune = true

[diff]
	indentHeuristic = true
[gpg]
	program = gpg2
[alias]
	graph = log --graph --oneline --all
	versiontag = "! awk '/^'$(git describe --tags --abbrev=0 | sed -e 's/^v//' -e 's/^package-v//')' / { exit } {print $0 }' CHANGELOG.rst | git tag -s -F -"
